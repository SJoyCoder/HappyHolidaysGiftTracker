<div id="giftBlock" >
  {{!-- {{!-- <h2>{{giftName}}</h2> --}}
  <p>{{{get_emoji}}}</p> 




  {{#each recGifts as |recGift|}}
  {{!-- added a style element not found in css --}}
  <div class="card" style= "margin-bottom: 40px;">
    <h1 style="font-weight:600; text-decoration: underline; font-size: x-large;">{{recGift.name}}{{{get_emoji}}}</h1>
    {{#each recGift.gifts as |gift|}}
    <p id="giftInfo">{{gift.giftName}} ${{gift.cost}} </p>
    <p>Store:{{gift.whereToBuy}},  Purchased?:{{gift.is_purchased}}</p>
    <button class="button grnbtn deleteButton" data-id="{{gift.id}}" id="deleteButton" >Delete</button>
    {{/each}}
</div>
{{/each}}
  {{!-- <ul>
    <li>{{is-purchased}}</li>
    <li>{{cost}}</li>
    <li>{{whereToBuy}}</li>
  </ul> --}}
  {{!-- <p>Created by {{user.username}} on {{format_date date_created}}</p> --}}
  {{!-- <p>
    <span for="img" aria-label="gift">🎁</span>
    <span class="dollar-amount">{{format_amount needed_funding}}</span>
    needed
  </p> --}}
  {{!-- <p>
    <button class="btn btn-primary">Fund this Gift!</button>
  </p> --}}
</div>
<script src="./js/gift.js"></script>