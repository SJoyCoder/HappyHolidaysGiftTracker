<!-- Loop over each object in the gifts array -->
{{#each gifts as |gift| }}
<div class="row mb-4 gift">
  <div class="col-md-5">
    <h2>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{{get_emoji}}}
      <a href="/gift/{{gift.id}}">{{gift.name}}</a>
    </h2>
    <p>
      <span for="img" aria-label="gift">🎁</span>
      <!-- Pass needed_funding value to the helper function -->
      {{!-- <span class="dollar-amount">{{format_amount gift.needed_funding}}</span> --}}
      {{!-- needed! --}}
    </p>
    <p>Created by {{gift.user.name}} </p>
  </div>
  <div class="col-md-7">
    <p>
      {{gift.description}}
    </p>
  </div>
</div>
{{/each}}
